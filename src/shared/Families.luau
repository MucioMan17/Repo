-- Family (faction) definitions. Rename, recolor, or add your own freely.
-- `id` is the stable key used in data and remotes; `name` is what players see.
return {
	{ id = "salvatore", name = "Salvatore", color = Color3.fromRGB(170, 40, 40) },
	{ id = "moretti",   name = "Moretti",   color = Color3.fromRGB(40, 90, 170) },
	{ id = "bianchi",   name = "Bianchi",   color = Color3.fromRGB(60, 150, 80) },
}
